#[derive(Debug, Eq, Hash, PartialEq, Clone, Copy)]
pub enum TokenKind {
    ILLEGAL,
    EOF,
    IDENT,
    INT,
    ASSIGN,
    PLUS,
    MINUS,
    BANG,
    ASTERISK,
    SLASH,
    COMMA,
    SEMICOLON,
    LPAREN,
    RPAREN,
    LBRACE,
    RBRACE,
    FUNCTION,
    LET,
    LT,
    LTE,
    GT,
    GTE,
    TRUE(bool),
    FALSE(bool),
    IF,
    ELSE,
    RETURN,
    EQ,
    NEQ,
    BLANK,
    STRING,
}
